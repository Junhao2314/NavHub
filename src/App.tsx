/**
 * App Module Re-export
 * App 模块重导出
 *
 * Re-exports the main AppContainer component and its props type.
 * 重导出主 AppContainer 组件及其属性类型。
 */

export type { AppProps } from './app/AppContainer';
export { default } from './app/AppContainer';
